pluginManagement {
    repositories {
        maven { url '/workspace/local-maven-repo' }
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.PREFER_SETTINGS)
    repositories {
        maven { url '/workspace/local-maven-repo' }
        google()
        mavenCentral()
    }
}
rootProject.name = "PdfStreamEdit"
include ':app'
